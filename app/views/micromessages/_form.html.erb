<%= form_for(@micromessage) do |f| %>
  <% if @micromessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micromessage.errors.count, "error") %> prohibited this micromessage from being saved:</h2>

      <ul>
      <% @micromessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :utilisateur_id %><br />
    <%= f.text_field :utilisateur_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
